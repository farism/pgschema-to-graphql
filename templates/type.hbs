{{pascalName}}Type = GraphQL::ObjectType.define do

  name "{{pascalName}}"
  description ""
{{#if hasIdField}}

  # relay node interface

  interfaces [NodeIdentification.interface]
  global_id_field :id
{{/if}}

  # scalars

{{#each fields}}
  field :{{#if property}}{{name}}{{else}}{{name}}{{/if}}, {{required}}types.{{scalarType}}, ""{{#if property}}, property: :{{columnName}}{{/if}}
{{/each}}

  # associations

{{#each associations}}
  field :{{name}}, types[{{type}}Type], ""
{{/each}}

end
